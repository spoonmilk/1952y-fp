How do the things?

Install uv

then:
`uv install`
`uv run stats-to-csv.py` on every workload directory, so `solomon/MD`, `solomon/DLP`, etc.
`uv run main.py`

figures are out as svgs in the `./figures` directory
