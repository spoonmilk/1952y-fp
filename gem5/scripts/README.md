# CSCI1952Y Final Project - Patrick, Alex, Jerome, Sas - Build/Run Scripts

## Things to do first

For some annoying compatibility reasons, you'll want to run
`sudo apt-get install -y python-is-python3` to fix incompatibilities
with the old-ass repos we use.

## Scripts/What they do

`compile-microbench.sh` - Compiles the microbench benchmarks for RISC-V
`run-test.sh` - Run a test of a single benchmark and the cahce
`mb-sweep.sh` - Run a bunch of microbenchmarks in one
